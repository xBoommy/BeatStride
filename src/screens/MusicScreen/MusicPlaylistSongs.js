import React, { } from 'react';
import { View, Text } from 'react-native';

const MusicPlaylistSongs = ({navigation}) => {
    return(
        <View>
            <Text>ExerciseMain</Text>
        </View>
    );
};

export default MusicPlaylistSongs;